# Agent Deployment Instructions

When assisting with this project, use the following commands for deployment to Vercel:

- **Staging / Preview**: Run `vercel` to create a preview deployment.
- **Production**: Run `vercel --prod --yes` to deploy directly to the production environment.

Ensure any significant changes are verified in the preview environment before pushing to production.
